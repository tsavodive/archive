# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# 빌드 산출물 (GitHub Actions에서 npm run build로 생성)
_sass/vendors
assets/js/dist

# Hidden system files
*~
.DS_Store
Thumbs.db

# 초안은 로컬에만 보관 (공개 저장소에 올라가지 않도록)
_drafts/

# 작업 기록은 로컬에만 보관 (공개 저장소에 올라가지 않도록)
# 사이트 빌드에서 빼는 것은 _config.yml 의 exclude 가 계속 담당합니다
WORKLOG.md
