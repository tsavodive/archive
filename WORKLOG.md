# 작업 기록

> 다음 작업을 시작할 때 이 파일부터 읽으면 됩니다.
> 일하는 방법은 [POSTING_GUIDE.md](POSTING_GUIDE.md) 에 정리되어 있습니다.

---

## 현재 상태 (2026-08-25 기준)

- 사이트: <https://tsavodive.github.io/archive/> — 정상 동작
- 저장소: `tsavodive/archive` (**공개 저장소**), 브랜치 `main`
- 로컬 경로: `C:\Users\mln\Desktop\장기프로젝트\개인페이지\archive`
- push하면 GitHub Actions가 2~4분 뒤 사이트를 갱신함
- 발행된 글: **4편** (「이렇게 일합니다」, 「변호사님은 끝까지 하실 수 있겠어요?」, 「그날의 상황을 다시 만들어보았습니다」, 「의뢰인의 말을 법적인 언어로 번역하는 일」)
- 구글 서치 콘솔: **소유확인 완료** (2026-08-24, 최상위 `googlefccb53f6e674748a.html` 파일 방식 — 지우지 말 것)

## 2026-08-24 ~ 25 에 한 일

1. **네 번째 글 발행** — 「의뢰인의 말을 법적인 언어로 번역하는 일」
   `_posts/2026-08-04-administrative-existence.md` (수행기록 › 행정, 태그 `행정소송`)
   주소: <https://tsavodive.github.io/archive/posts/administrative-existence/>
   직접 쓰신 글입니다. 발행 전에 front matter 의 `date` 가 `2026-08-05` 로 되어 있어
   파일 이름(`2026-08-04-`)과 어긋나기에 **`date` 를 `2026-08-04` 로 맞췄습니다.**
   앞선 세 글(08-01·02·03)의 연속이 되도록 맞춘 것입니다.
2. **누수 글 수정** — 의뢰인의 마지막 당부를 큰따옴표 직접인용에서 간접화법으로 바꾸고,
   앞 문장의 '마지막에 이렇게' 를 덜어냈습니다.
3. **이력 페이지 수정** — 법무법인 윈 항목 첫 문장에 '소속변호사(이른바 어쏘변호사)로서' 를
   넣어, 사건을 배당받아 수행한다는 뒤 설명의 전제가 드러나도록 했습니다.
4. **행정 글·이력 페이지 2차 수정** — 사건이 특정될 여지를 줄였습니다 (아래 별도 항목)
5. **구글 서치 콘솔 등록** — HTML 파일 방식 (아래 별도 항목)
6. **누수 글에서 시점 표현 삭제** — 첫 문장을 통째로 덜어냈습니다 (아래 별도 항목)
7. **배포 워크플로에 `WORKLOG.md` 추가** — 이 파일만 고쳤을 때 헛빌드가 돌지 않도록
   [`.github/workflows/pages-deploy.yml`](.github/workflows/pages-deploy.yml) 의
   `paths-ignore` 에 한 줄 넣었습니다 (아래 별도 항목)

### 파일 이름의 날짜는 주소에 안 나옵니다 (확인 완료)

[`_config.yml:196`](_config.yml) 의 `permalink: /posts/:title/` 때문에 글 주소에는
**날짜가 들어가지 않습니다.** `2026-08-04-administrative-existence.md` 의 주소는
`/posts/administrative-existence/` 입니다.

- 파일 이름의 **날짜 부분**은 나중에 바꿔도 주소가 그대로입니다
  (다만 Jekyll 이 `연-월-일-제목.md` 형식을 요구하므로 형식 자체는 지켜야 합니다)
- **제목 부분(`administrative-existence`)을 바꾸면 주소가 바뀝니다.** 이미 발행한 글은
  건드리지 않는 편이 좋습니다
- 화면에 보이는 게시일과 글 정렬은 front matter 의 `date` 가 정합니다

### 사건이 특정될 여지를 줄였습니다

저장소가 공개인 상태에서 검색 노출까지 되면, **소속·시기·특이한 사건 유형이 결합될 때
사건이 좁혀질 수 있다**는 점이 확인되어 두 파일을 고쳤습니다.

| 어디 | 전 | 후 |
| --- | --- | --- |
| 행정 글 | "행정상 건축물대장의 부분말소 처분이 무효임을 확인해 달라는 내용으로 청구를 정리" | "청구와 주장내용을 정리했습니다" |
| 행정 글 | "처음으로 직접 준비서면을 작성했던 사건" | "수습변호사로 일하던 기간에 서면을 작성했던 사건" |
| 행정 글 | 사무실 변호사들이 아직 파악하지 못한 상태였다는 서술 | 덜어냄 |
| `career.md` | "건축물대장직권말소처분취소 등 행정사건" | "일반적인 민·형사 사건, 행정사건" |
| 누수 글 | "제가 회사에 들어간 지 얼마 되지 않은 2021년 11월이었습니다." | 문장을 통째로 삭제 (08-25) |

이제 저장소 어디에도 처분명은 남아 있지 않습니다. 글 본문에 자료를 나열한
'지적도와 건축물대장, 몇 년에 걸친 항공사진' 한 곳이 남았는데, 처분명이 아니라
대조한 자료의 나열이라 특정에 쓰이기는 어렵습니다.

> **사이트에서 지운 문장도 커밋 이력에는 남습니다.** 저장소가 공개라 이전 판을 누구나
> 꺼내볼 수 있습니다(수정 전 문장은 `8aac7af` 에 있습니다). 의뢰인에 관한 서술은
> **올리기 전에** 한 번 더 보는 편이 확실합니다. 올린 뒤에 지우는 것은 절반만 지우는 것입니다.
{: .prompt-warning }

### 시점은 글이 아니라 `career.md` 가 알려줍니다

누수 글에서 '2021년 11월' 을 지우는 김에 전체를 훑어봤더니, **글에서 날짜를 지워도
이력 페이지와 나란히 놓으면 시점이 복원된다**는 것이 확인되었습니다.

| 글에 남는 표현 | `career.md` | 결합하면 |
| --- | --- | --- |
| "회사에 들어간 지 얼마 되지 않은 시점" | `2021. 11. – 현재` 법무법인 윈 | 2021년 11~12월경 |
| "수습변호사로 일하던 기간에" (행정 글) | `2021. 4. – 2021. 10.` 법무법인 로앤 | 7개월 구간으로 특정 |

**`career.md` 의 날짜는 낮추지 않기로 했습니다.** 이력 페이지는 정확한 것이 제 역할이고,
연 단위로 뭉개면 이력으로서의 신뢰도만 깎입니다. 대신 **글 쪽에서 경력 단계를 암시하는
표현을 빼는 방향**으로 정리했습니다. 그래서 누수 글은 첫 문장("회사에 들어간 지 얼마 되지
않은 시점이었습니다")을 통째로 덜어내고 "어느 날 한 의뢰인에게 전화를 걸어…" 로 시작합니다.
문장이 없어도 글이 자연스럽고, 시점 단서만 사라집니다.

형사 글에는 시점 표현이 없습니다('당시' 는 전부 사건 내부 시점이라 무관합니다).
행정 글의 "수습변호사로 일하던 기간에" 는 아직 남아 있습니다 — 아래 '다음에 할 일' 참고.

> **줄만 바꾸면 문단이 나뉘지 않습니다.** 날짜를 지우면서 빈 줄까지 지우면 두 문장이
> 한 문단으로 붙어 나옵니다. 문단을 나누려면 **빈 줄 하나**, 줄만 바꾸려면 **줄 끝에 공백 두 칸**
> 또는 `<br>` 이 필요합니다 (누수 글 28행이 `<br>` 을 쓰고 있습니다).
{: .prompt-tip }

**가사 글을 쓰실 때 같은 점을 보셔야 합니다.** `career.md` 에 '친양자입양신청,
친권자변경신청, 상속재산분할' 이 그대로 나열되어 있어서, 이 중 하나를 소재로 글을 쓰면
같은 방식의 결합이 다시 생깁니다 (`_drafts/2026-08-04-family-divorce.md`).

### 구글 서치 콘솔 등록 — HTML 파일 방식

GitHub 웹에서 `googlefccb53f6e674748a.html` 을 저장소 최상위에 직접 올리셨습니다(`f63f0a4`).
Jekyll 은 front matter 가 없는 파일을 손대지 않고 그대로 복사하므로 그대로 배포됩니다.
<https://tsavodive.github.io/archive/googlefccb53f6e674748a.html> — 정상 응답(200) 확인했습니다.

- **이 파일은 지우면 안 됩니다.** 구글이 주기적으로 다시 확인하고, 사라지면 소유확인이 풀립니다
- `_config.yml` 의 `webmaster_verifications: google:` 칸은 **비워 두어도 됩니다.**
  파일 방식으로 이미 확인되므로 둘 다 할 필요는 없습니다

> **웹에서 파일을 올리신 뒤에는 로컬에서 `git pull` 을 먼저 하세요.**
> 이번에 웹 업로드와 로컬 커밋이 어긋나 push 가 한 번 막혔습니다.
> `git pull --rebase origin main` 으로 풀었고 충돌은 없었습니다.
{: .prompt-tip }

### `WORKLOG.md` 는 사이트에 배포되지 않습니다 (확인 완료)

두 가지가 별개입니다. 헷갈리기 쉬워 적어둡니다.

| | 무엇을 정하는가 | `WORKLOG.md` |
| --- | --- | --- |
| `_config.yml` 의 `exclude:` | **사이트에 포함할지** | 들어 있음 → 배포 안 됨 (`/archive/WORKLOG.md` 는 404 확인) |
| 워크플로의 `paths-ignore:` | **push 가 빌드를 돌릴지** | 빠져 있었음 → 이 파일만 고쳐도 빌드가 헛돌았음 |

2026-08-25 에 `paths-ignore` 에 `WORKLOG.md` 한 줄을 넣어 맞췄습니다.
`POSTING_GUIDE.md` 와 `chirpy_archive_project_handoff.md` 는 원래 들어 있었고,
`WORKLOG.md` 만 빠져 있던 것입니다.

**다만 사이트에 안 나온다고 비공개인 것은 아닙니다.** 저장소가 공개라 파일 자체는 그대로 보입니다
(아래 '다음에 할 일' 의 내부 문서 항목).

### 공개 범위 점검 (트래픽·보안, 확인 완료)

| 항목 | 결론 |
| --- | --- |
| 트래픽 | **문제 없음.** GitHub Pages 한도는 사이트 1GB · 대역폭 100GB/월(soft) · 빌드 10회/시간(soft) 이고, 현재 사이트는 **1.9MB** 입니다. 방문당 1MB 를 잡아도 월 10만 방문까지 여유가 있고, 넘어도 과금이 아니라 제한입니다 |
| 기술적 보안 | **공격 대상이 거의 없음.** 정적 사이트라 서버·데이터베이스·로그인·입력폼이 없습니다. 저장소에 키·비밀번호가 없고, 댓글 기능이 꺼져 있으며(`comments.provider` 비어 있음), 애널리틱스가 없어 방문자 정보를 수집하지 않습니다(개인정보처리방침 부담도 없음) |
| `_drafts/` | `.gitignore` 에 들어 있어 **실제로 하나도 올라가 있지 않습니다** (확인함) |
| 남은 것 | '다음에 할 일' 에 세 줄 추가해 두었습니다 |

GitHub Pages 이용약관은 '상거래를 주로 매개하는 사이트'로 쓰는 것을 금지합니다.
지금처럼 이력과 기록을 소개하는 형태는 해당하지 않지만, **상담 접수 폼이나 비용 결제를
붙이면 성격이 달라집니다.**

## 2026-08-12 에 한 일

1. **자세한 이력 페이지 신설** — `career.md` (최상위, 새 파일)
   주소: <https://tsavodive.github.io/archive/career/>. 경력 3건 + 학력 2건을 문단으로 풀어 쓴 페이지입니다.
   초안 `_drafts/2026-07-31-etc.md` 를 글(post)로 쓰셨던 것을 **페이지(page)로 옮겼습니다.**
   내용은 그대로 두고 오타 하나만 고쳤습니다 (`친권자변경신쳥` → `친권자변경신청`).
   옮긴 뒤 **초안 파일은 삭제했습니다** (내용이 전부 `career.md` 로 갔고, 두면 미리보기에 두 번 나옴).
2. **소개 페이지에서 링크 연결** — `_tabs/about.md`
   맨 아래 `[링크]` 를 `[링크]({{ site.baseurl }}/career/)` 로 바꿨습니다.
   같은 날 `## 이력` → `## 경력`, 소개 문단 한 곳(`평소 스스로 만드는 것`)도 함께 고치셨습니다.

### 왜 `_tabs` 가 아니라 최상위에 두었나

`_tabs/` 에 넣으면 [`_includes/sidebar.html`](_includes/sidebar.html) 이 **왼쪽 메뉴에 자동으로 추가합니다.**
이 페이지는 소개 안에서만 들어가면 되므로 메뉴에 나올 필요가 없어, 최상위에 두고
머리말에 `layout: page` 와 `permalink: /career/` 를 직접 적었습니다.
(`_tabs/` 파일들은 `_config.yml` 의 `defaults` 가 이 두 가지를 자동으로 붙여 줍니다)

메뉴가 `소개 / 전체 글 / 주제별 보기 / 키워드별 보기 / 날짜별 보기` 그대로인 것을 빌드로 확인했습니다.

### 게시날짜·작성자·읽는 시간이 안 붙는 이유

`layout: page` 이면 [`_layouts/page.html`](_layouts/page.html) 이 **제목과 본문만** 그립니다.
날짜·작성자·읽는 시간은 `layout: post` 인 글에만 붙습니다. 따로 끌 것이 없습니다.

한 가지, **페이지에는 목차(TOC)도 안 나옵니다.** 지금 길이면 없어도 읽기 좋습니다.

말을 **"자세한"으로 통일했습니다.** 페이지 제목도 소개의 링크 문장도 "조금 더 자세한 이력" 입니다.

## 메뉴 순서 배포 — 확인 완료 (2026-08-11)

지난번에 미확인으로 남겨 두었던 건입니다. **정상 배포되었습니다.**

사이트의 첫 아이콘이 `fa-info-circle` 로 나오는 것을 확인했습니다.
순서는 `소개 → 전체 글 → 주제별 보기 → 키워드별 보기 → 날짜별 보기` 입니다.

지난번에 "반영 안 됨"으로 보였던 것은 **빌드가 아직 안 끝났던 것**이 맞았습니다.
그때 확인한 `Last-Modified` 가 `2026-08-10 15:13 GMT` 였는데,
지금은 `15:23 GMT` 로 커밋 `4d92839` 시각(한국시간 08-11 00:22)과 맞아떨어집니다.
**몇 분 더 기다렸으면 됐을 일입니다.** 다음에도 push 직후 안 보이면 2~4분 기다렸다 다시 보세요.

## 2026-08-10 에 한 일

1. **메뉴 이름 변경** — `_data/locales/ko-KR.yml`
   홈/카테고리/태그/아카이브/정보 → **전체 글 / 주제별 보기 / 키워드별 보기 / 날짜별 보기 / 소개**
2. **프로필 사진 적용** — `assets/img/avatar.jpg` + `_config.yml` 의 `avatar` 경로 지정
   (깃허브 웹에서 `assets/avatar.jpg` 로도 올라갔던 중복 파일은 삭제함)
3. **연락처 아이콘 정리** — `_data/contact.yml`
   깃허브 아이콘 삭제, 개인 홈페이지(지구본)·유튜브 추가. 푸터 저작권 링크도 홈페이지로 변경
   (유튜브는 같은 날 저녁에 다시 내렸습니다. 지금 보이는 것은 홈페이지·이메일·RSS 세 개)
4. **글 초안 9개 생성** — `_drafts/` (아래 참고)
5. **초안을 로컬 전용으로 전환** — `.gitignore` 에 `_drafts/` 추가하고 기존 추적 파일 5개 추적 해제
6. **POSTING_GUIDE.md 보강** — 분류 체계 절, 초안 공개 범위 절 추가
7. **첫 정식 글 발행** — `2026-08-01-work-ethics.md` (「이렇게 일합니다」)
   `_drafts/` → `_posts/` 로 옮기고 push. 주소: <https://tsavodive.github.io/archive/posts/work-ethics/>
   이후 제목에서 `(직업관)` 을 빼고, 분류를 `[이력, 변호사 업무]` + 태그 `송무` `직업관` 으로 확정
   (제목을 바꿔도 주소는 그대로입니다. 주소는 제목이 아니라 **파일 이름**에서 만들어집니다)

8. **「블로그를 시작합니다」 발행 취소** — `_posts/` → `_drafts/2026-08-09-blog-open.md`
   내용이 아직 `[대괄호]` 템플릿 상태였음. 홈 상단 고정(`pin: true`)도 함께 사라짐.
   파일은 남아 있으므로 내용을 채운 뒤 다시 `_posts/` 로 옮기면 됩니다
9. **분류 체계 개편** — 카테고리를 아래 구조로 확정하고 초안 전체에 반영
   - `수행기록` › 민사 / 형사 / 가사 / 행정
   - `이력` › 변호사가 되기 전 / 변호사 업무
   - `소식` (소분류 없음)
   - `단상` (소분류 없음)

   바뀐 것: `변호사가 되기까지` → `변호사가 되기 전`, `지금 하는 일` → `변호사 업무`,
   `[단상, 진로]` 와 `[단상, 상담실에서]` 는 소분류를 없애고 `[단상]` 하나로 합침

10. **날짜별 보기를 동양식으로 변경** — `_layouts/archives.html` + `_data/locales/ko-KR.yml`
    `10 / 08` (일 → 월) 에서 `08 / 10` (월 → 일) 로. 글씨 크기·색은 그대로 두고 순서만 바꿈.
    두 파일을 같이 고쳐야 합니다 (POSTING_GUIDE 7절 참고)

11. **기초 서식 확정** — 아래 "기초 서식" 절 참고

## 2026-08-11 에 한 일

1. **소개 페이지 발행** — `_tabs/about.md`
   이력·학력 표, 연락처(이메일) 추가. 소개 문단은 `<br>` 로 줄바꿈
2. **파비콘을 프로필 사진으로 교체** — `assets/img/favicons/` 6개 파일
   `avatar.jpg`(512x512) 에서 생성. **`favicon.svg` 를 빼먹으면 크롬에서 옛 그림이 그대로 보입니다**
   (크롬이 SVG 를 가장 먼저 찾기 때문). 재생성은 `tools/make-favicons.ps1`
3. **메뉴 순서 변경** — `_includes/sidebar.html` + `_tabs/about.md`
   소개를 맨 위로. **배포 확인 완료** (위 "메뉴 순서 배포" 절 참고)
4. **검색엔진 수집에 관한 확인** — 아래 절 참고
5. **두 번째 글 발행** — `2026-08-02-civil-leakage.md`
   「변호사님은 끝까지 하실 수 있겠어요?」 누수 손해배상 사건 수행기록.
   분류 `[수행기록, 민사]`, 태그 `손해배상` `누수` `감정` `입증`.
   주소: <https://tsavodive.github.io/archive/posts/civil-leakage/> (커밋 `eeff03c`)

   옮기면서 손댄 곳: 태그 `증명` → **`입증`** 으로 바꿨습니다.
   뜻이 같은 태그를 새로 만들면 키워드별 보기에서 글이 두 곳으로 갈라지기 때문입니다
   (POSTING_GUIDE 2절). `입증` 은 원래 쓰기로 정해 둔 태그입니다.

6. **누수 글 수정 발행 (2회)** — 커밋 `420bd16`, `0121dcc`
   - `description` → 「직접 보고, 끝까지 확인합니다」
   - 빗물 인용문 앞에 `<br>` (‘오늘도 비가 오는데…’ 가 새 줄에서 시작)
   - 마지막 두 문단 구조 정리, 「자주 연락드리고…」를 독립 문단으로
   - 이중 천장 대목에 「옥상에서 스며들어온」 을 넣어 유입 경위 보완

   목록 네 곳에 안 보이던 것은 브라우저 캐시였습니다 (위 "목록이 옛날 그대로인 현상" 절)

   > **원고를 고칠 때 어느 폴더인지 보고 하세요.** 이 글은 발행한 뒤라
   > 파일이 `_posts/` 에 있습니다. `_drafts/` 에 있는 줄 알고 고치면,
   > 실은 **공개된 글을 직접 고치고 있는 것**입니다. 저장만으로 공개되지는 않지만
   > (push 를 해야 반영됨), `git add .` 을 습관적으로 치면 함께 올라갑니다.
   > 비공개로 손보려면 `_drafts/` 로 도로 옮긴 뒤 고치고, 다 되면 다시 `_posts/` 로.
   {: .prompt-warning }

7. **세 번째 글 발행** — `2026-08-03-criminal-lethal.md`
   「그날의 상황을 다시 만들어보았습니다」 항소심 형사사건 수행기록.
   분류 `[수행기록, 형사]`, 태그 `형사변호` `양형` `상담`.
   주소: <https://tsavodive.github.io/archive/posts/criminal-lethal/> (커밋 `ae4d2fa`)

   **발행 직후 태그를 직접 고치셨습니다.** `형사변호` `양형` `상담` → **`형사`** **`증명(입증)`**
   (수정본을 `_drafts/` 에 만들어 두셨기에, 제가 `_posts/` 의 발행본에 옮겨 넣고
   중복된 `_drafts/` 사본은 지웠습니다. 같은 이름의 파일이 두 폴더에 있으면 헷갈립니다)

   옮기기 전에 두 가지를 여쭤 확정했습니다.
   - 파일 이름 `criminal-lethal` **그대로 유지** → 주소가 `/posts/criminal-lethal/` 로 굳었습니다.
     본문 내용은 '위험한 물건' 쪽이지만 파일 이름은 두신 대로 갑니다
   - 맨 끝 고지 문구 **한 줄 그대로**. 누수 글은 두 줄이라 수행기록끼리 서식이 다릅니다.
     나중에 통일하시려면 아래 '다음에 할 일' 참고

   이 글로 새로 생긴 것: 주제별 보기에 `형사`, 키워드별 보기에 `형사변호` `양형` `상담`.
   `상담` 은 POSTING_GUIDE 2절에서 "형사·가사·단상에 걸쳐 다는 태그"로 정해 둔 것인데
   **이 글이 첫 사용**입니다. 앞으로 상담 이야기가 나오는 글에 같은 태그를 다세요

8. **`입증` 태그를 `증명(입증)` 으로 통일** — 발행된 글·초안·가이드 전부
   - `_posts/2026-08-02-civil-leakage.md` (누수 글)
   - `_posts/2026-08-03-criminal-lethal.md` (형사 글)
   - `_drafts/2026-08-02-civil-damages.md` (초안, **태그 줄만**. 제목·본문의 '입증' 은 그대로 둠)
   - `POSTING_GUIDE.md` 2절 태그 참고표

   **괄호는 주소에서 하이픈이 됩니다.** `증명(입증)` → `/tags/증명-입증/` 로 깔끔하게 나옵니다.
   빌드해서 확인했고, 두 글이 한 페이지에 같이 모입니다. 옛 `/tags/입증/` 페이지는 사라졌습니다.

   > 08-11 오전에 `증명` → `입증` 로 바꿨던 것을 이날 `증명(입증)` 로 다시 정했습니다.
   > 앞으로는 이 표기 하나만 쓰시면 됩니다.
   {: .prompt-info }

9. **색 바꾸기는 다음 기회로 미룸** — 아래 '다음에 할 일' 에 정리해 두었습니다.
   `_sass/themes/` 는 **아무것도 건드리지 않았습니다.**

### 확인이 필요한 것 — 형사 글의 `형사` 태그

형사 글의 분류가 `[수행기록, 형사]` 인데 태그에도 `형사` 가 있습니다.
POSTING_GUIDE 2절에 **"카테고리에 이미 있는 말은 태그로 또 달지 않는다"** 고 정해 두신 경우입니다.

이렇게 되면 아래 두 페이지의 글 목록이 **완전히 같아집니다.**

- 주제별 보기 `/categories/형사/`
- 키워드별 보기 `/tags/형사/`

말씀하신 대로 두었습니다. 군더더기라고 판단되시면 태그에서 `형사` 만 빼면 됩니다.

## 검색엔진 수집 (2026-08-24 Search Console 등록 완료)

**크롤링·색인과 Search Console 소유확인은 별개입니다.**
구글이 넣으라는 `<meta name="google-site-verification">` 코드는 색인을 시키는 코드가 아니라,
"이 사이트가 내 것"임을 증명해 검색 데이터를 보고 사이트맵을 제출하기 위한 열쇠입니다.
그 코드가 없어도 공개된 페이지는 색인될 수 있습니다.

이 사이트의 상태입니다.

| 항목 | 상태 |
| --- | --- |
| `robots.txt` | `Disallow: /norobots/` 뿐. 나머지는 전부 수집 허용 |
| `noindex` 태그 | 없음 |
| `sitemap.xml` | 자동 생성됨(글이 늘면 자동으로 반영), `robots.txt` 가 위치를 알려줌 |
| 소유확인 | **완료** (2026-08-24). 최상위 `googlefccb53f6e674748a.html` **파일 방식**. `_config.yml` 의 `webmaster_verifications` 는 비어 있어도 됩니다 |

2026-08-24 에 Search Console 등록을 마쳤으므로, 이제 **문도 열려 있고 구글에도 알린**
상태입니다. 색인은 바로 되지 않고 보통 며칠에서 몇 주가 걸립니다.
현재 상태는 구글에서 `site:tsavodive.github.io/archive` 로 검색해 확인할 수 있습니다.

- 색인을 앞당기려면: Search Console 에서 사이트맵(`/archive/sitemap.xml`) 제출
- 나중에 특정 글을 검색에서 빼려면: Search Console 의 삭제 요청 도구 → 그다음
  글 front matter 에 `sitemap: false` 또는 `robots.txt` 로 조치.
  **등록해 두었기 때문에 이 통제 수단이 생긴 것입니다**
- 다만 **검색에서 내려도 저장소 커밋 이력은 그대로 남습니다.** 둘은 별개입니다

## 기초 서식 (2026-08-10 확정)

**모든 글에 자동으로 적용됩니다.** 글마다 따로 해줄 일은 없습니다.
테마 자체를 고친 것이라, 앞으로 `_posts/` 에 새 글을 넣기만 하면 같은 서식으로 나옵니다.

| 항목 | 확정된 모양 | 고친 파일 |
| --- | --- | --- |
| 제목 아래 한 줄 요약(`description`) | 1rem·회색. 게시정보(0.85rem)보다 크고 원래(1.125rem)보다 작음 | `_sass/pages/_post.scss` |
| 게시일·작성자 | `2026/08/01 게시 · 이미린 씀` — **한 줄에 나란히** | `_layouts/post.html` + `ko-KR.yml` |
| 수정일 | **아예 쓰지 않음** (2026-08-12 부터) | `_layouts/post.html` + `_plugins/posts-lastmod-hook.rb` |
| 오른쪽 '최근 업데이트' 패널 | **게시순** 정렬 (수정순 아님) | `_plugins/posts-lastmod-hook.rb` |
| 읽는 시간 | `읽는 시간 10분` (라벨 → 숫자) | `_includes/read-time.html` |
| 모바일 '바로가기' 버튼 | 본문 위 **오른쪽** 정렬 | `_sass/pages/_post.scss` |
| 본문 문단 사이 여백 | 1rem(부트스트랩 기본) → **1.4rem** | `_sass/base/_base.scss` |

> 문단 여백은 **원고의 빈 줄로는 조절되지 않습니다.** 마크다운은 빈 줄이 하나든 셋이든
> 문단 구분 하나로만 셉니다. 간격을 바꾸려면 위 `_base.scss` 의 `.content > p` 값을 고치세요.

> **수정일 기능을 통째로 껐습니다 (2026-08-12).** 본문을 고칠 때뿐 아니라
> **태그나 카테고리만 손봐도 커밋이 쌓여 전부 '수정'으로 집계되는 것**이
> 실제와 맞지 않았습니다. `_plugins/posts-lastmod-hook.rb` 의 훅을 주석 처리했고,
> 그 결과 오른쪽 '최근 업데이트' 패널이 **게시순**으로 정렬됩니다.
> 되살리려면 그 파일의 주석을 풀면 됩니다.
{: .prompt-info }

> **수정일은 원고에 적는 값이 아니었습니다.** `_plugins/posts-lastmod-hook.rb` 가
> **깃 커밋 이력**에서 뽑아냅니다(커밋이 2개 이상인 글에만 붙음). 그래서 원고에서
> 지울 것이 없고, `_layouts/post.html` 에서 표시를 없애면 모든 글에 한꺼번에 적용됩니다.
>
> 가운데점(`·`)은 글자가 아니라 `_sass/pages/_post.scss` 의
> `.post-meta span + span::before` 가 붙여 줍니다. **두 `<span>` 이 나란히 있어야**
> 점이 생기므로, 머리말을 다시 손볼 때 순서를 바꾸지 마세요.
{: .prompt-info }

라벨 글자 자체(`게시`·`업데이트`·`씀`)는 `_data/locales/ko-KR.yml` 의 `post:` 아래에 있습니다.
**어순은 그 파일이 아니라 `_layouts/post.html` 에서 정합니다.** 라벨만 바꾸면 위치는 그대로입니다.

## 목록이 옛날 그대로인 현상 — 올릴 때도, 내릴 때도 (원인 확인 완료)

사이트 문제가 아니라 **브라우저에 저장된 사본** 때문입니다.
`_config.yml` 의 `pwa.cache.enabled: true` 설정으로 아래 다섯 페이지가 방문자
브라우저에 저장됩니다. 개별 글 본문은 저장되지 않아서, 목록에는 옛 제목이 보이는데
눌러 보면 404 가 뜹니다.

`/` · `/categories/` · `/tags/` · `/archives/` · `/about/`

`Ctrl + Shift + R` 로 해결됩니다. 자세한 것은 POSTING_GUIDE 8절.
서버(github.io)에는 지운 글의 흔적이 전혀 없음을 확인했습니다.

**2026-08-11 추가 — 반대 방향으로도 똑같이 나타납니다.**
글을 새로 올렸는데 목록 네 곳(전체 글·주제별·키워드별·날짜별)에 **전혀 안 보이는** 증상을
겪었습니다. 원인은 같습니다. 저 네 페이지가 브라우저에 저장돼 있어 옛 사본이 나온 것입니다.

헷갈리기 쉬운 점이 하나 있습니다. **글 하나하나의 본문은 저장되지 않기 때문에**,
다른 글을 열면 그 페이지 오른쪽 '최근 업데이트' 에는 새 글이 멀쩡히 보입니다.
그래서 "글은 올라갔는데 목록에서만 빠졌다"는 것처럼 보입니다. 사이트 문제가 아닙니다.

| 어디서 보나 | 저장되나 | 새 글이 보이나 |
| --- | --- | --- |
| 목록 네 곳 + 소개 | **저장됨** | 안 보임 (옛 사본) |
| 글 본문 페이지·오른쪽 '최근 업데이트' | 저장 안 됨 | **보임** |

확인 방법입니다. 서버가 맞는지부터 봅니다. 아래가 4 로 나오면 서버는 정상이고
브라우저만 옛날 것을 보고 있는 것입니다.

```powershell
'', 'categories/', 'tags/', 'archives/' | ForEach-Object {
  (curl.exe -s -H "Cache-Control: no-cache" "https://tsavodive.github.io/archive/$_") -match 'civil-leakage'
} | Where-Object { $_ } | Measure-Object | Select-Object -ExpandProperty Count
```

**목록 페이지 각각에서** `Ctrl + Shift + R` 을 누르면 해결됩니다.
한 페이지에서 눌렀다고 나머지 세 곳이 같이 갱신되지는 않습니다.

## 카테고리와 태그에 같은 이름을 써도 되는지 (확인 완료)

**써도 됩니다.** 빌드해서 확인했습니다. 둘은 주소가 갈라져 서로 다른 목록으로 만들어집니다.

- `/categories/민사/` (주제별 보기) 와 `/tags/민사/` (키워드별 보기) 가 각각 생성됨
- 카테고리만 `민사` 인 글은 앞쪽에만, 태그도 `민사` 인 글은 양쪽에 나옴

기준: **그 글의 카테고리에 이미 있는 말은 태그로 또 달지 않습니다.**
다른 카테고리 글인데 그 주제를 다룰 때만 답니다.
(예: `[단상]` 글이 민사 이야기를 하면 `민사` 태그를 다는 것이 유용)

`소식` 과 `단상` 은 소분류가 없으므로 **태그가 소분류 역할을 대신합니다.**

## 다음에 할 일

- [x] ~~「이렇게 일합니다」에 `상담` 태그 추가~~ → **넣지 않기로 함** (2026-08-10). 태그는 `송무` `직업관` 2개로 확정
- [ ] 취미·관심사 글의 분류. 당분간 `[단상]` + `취미`·`관심사` 태그로 모아두다가,
      같은 태그가 3편이 되면 그때 `[이력, 일 밖에서]` 소분류를 만들지 결정
- [x] ~~**메뉴 순서 배포 확인**~~ → **정상 배포 확인 완료** (2026-08-11)
- [ ] `_drafts/2026-08-09-blog-open.md` 내용 채운 뒤 `_posts/` 로 다시 옮기기
- [ ] `_drafts/` 초안 중 완성한 것부터 `_posts/` 로 옮기기
- [ ] **맨 끝 고지 문구를 통일할지 정하기.** 지금 누수 글은 두 줄, 형사 글은 한 줄입니다
      (2026-08-11 에 한 줄 그대로 두기로 하고 발행함)
- [ ] 누수 글 18행(「누수 피해는 몇 년째…」 + 「한참 화를 내시던…」, 2026-08-24 수정본)과
      맨 끝 고지 문구 두 줄은 아직 붙어 나옵니다. 줄 끝 공백이 하나라 줄바꿈이 안 됩니다
      (두 칸이어야 `<br>` 이 됩니다). 그대로 둘지 정하기
- [x] ~~발행한 글의 `description` 을 글에 맞게 다시 쓰기~~ → **완료** (2026-08-11)
- [ ] `_drafts/00-글-템플릿.md` 의 예시값(`대분류`/`소분류`, `태그1`/`태그2`)을 실제 분류로 교체
- [ ] 사무실 주소·전화번호 확정되면 `_data/contact.yml` 의 주석 처리된 전화·지도 항목 활성화
- [ ] 인스타그램 계정 주소 확정되면 `_data/contact.yml:25-27` 주석 해제
- [ ] 유튜브를 다시 띄우려면 `_data/contact.yml:11-13` 주석 해제 (2026-08-10 에 내림)
- [x] ~~파비콘이 아직 Chirpy 기본값~~ → **프로필 사진으로 교체 완료** (2026-08-11)
      사진을 바꾸면 `tools/make-favicons.ps1` 을 다시 돌리면 됩니다 (POSTING_GUIDE 4절)
- [ ] **커밋 작성자 이메일 비공개로 돌리기** — 지금 공개 이력에 `berceuse16@gmail.com` 과
      `mln.lee@daum.net` 두 개가 드러나 있습니다. GitHub 설정에서 이메일 비공개를 켜고
      `git config user.email` 을 `...@users.noreply.github.com` 으로 바꾸면 **이후 커밋부터**
      가려집니다 (지난 커밋은 이력을 다시 쓰지 않는 한 그대로 남습니다)
- [ ] **내부 문서 3개를 계속 공개할지 정하기** — `WORKLOG.md`, `POSTING_GUIDE.md`,
      `chirpy_archive_project_handoff.md` 는 사이트에는 안 나오지만 **공개 저장소에서는
      그대로 보입니다.** 로컬 경로, 되돌린 글, 미발행 초안 제목까지 적혀 있습니다.
      빼려면 `git rm --cached` + `.gitignore` (역시 지난 이력에는 남음)
- [ ] **행정 글의 "수습변호사로 일하던 기간에" 를 그대로 둘지 정하기** (2026-08-25 보류)
      `career.md` 의 `2021. 4. – 2021. 10.` 과 결합하면 7개월 구간으로 좁혀집니다.
      빼면 시점 단서가 사라지지만, 이 글은 **초보 시절의 배움**이 서사의 핵심이라
      함께 밋밋해집니다. 뒤의 "처음 맡아보는 행정 사건이었던 데다" 는 연도로 환산되지
      않으므로 남겨도 됩니다
- [ ] **가사 글을 발행할 때** `career.md` 의 가사사건 나열(친양자입양신청·친권자변경신청·
      상속재산분할)과 글이 결합되어 사건이 좁혀지지 않는지 확인 (2026-08-24 행정 글과 같은 문제)

### 사이드바 색을 `#E3DBD7` 로 바꾸기 (2026-08-12 보류, 조사는 끝남)

바꿀 곳은 [`_sass/themes/_light.scss`](_sass/themes/_light.scss) **39행 한 줄**입니다.
`--sidebar-bg: #f6f8fa;` → `#E3DBD7`. 밝은 모드 색 변수는 모두 86개(어두운 모드 89개)이고
이 파일 하나에 다 있습니다.

다만 **한 줄만 바꾸면 곤란한 곳이 네 군데** 있습니다. 미리 조사해 두었습니다.

| # | 무엇이 문제인가 | 어디 | 권하는 값 |
| --- | --- | --- | --- |
| 1 | **홈 화면 페이지 번호 버튼**이 같이 바뀜. `--btn-paginator-hover-color: var(--sidebar-bg)` 라서 사이드바 색을 그대로 따라감 | `_light.scss:58` | `#f6f8fa` 로 고정 |
| 2 | 마우스 올렸을 때 색이 **파란기**. 베이지 위에서 어긋나 보임 | `--sidebar-hover-bg` | `rgb(206 193 186 / 64%)` |
| 3 | **부제(한 줄 소개)** 대비가 4.58 → **3.57** 로 기준(4.5) 미달 | `--site-subtitle-color` | `#5f5f5f` |
| 4 | **아래쪽 아이콘** 대비가 3.08 → **2.40** 으로 더 나빠짐 (원래도 미달) | `--sidebar-btn-color` | `#6f6f6f` |

통과하는 것: 메뉴 글자 `#545454` (5.55), 현재 메뉴 `#1d1d1d` (12.35), 블로그 제목(큰 글자라 3.57 로 충분).

취향에 따라 볼 것: `--sidebar-btn-bg: white`(아래 버튼)와 `--avatar-border-color: white`
(프로필 사진 테두리)는 베이지 위에서 도드라집니다. 테두리는 오히려 예쁠 수 있어 눈으로 보고 정할 것.

**어두운 모드는 따로입니다.** 같은 계열로 맞추려면 [`_dark.scss`](_sass/themes/_dark.scss) 42행
(`--sidebar-bg: #1e1e1e`)도 함께 고쳐야 합니다. 예: 짙은 갈색 `#2a2320`.

> **이용약관은 문제없습니다.** Chirpy 는 MIT 라이선스라 색·레이아웃·글꼴 무엇이든
> 고쳐도 되고 공개해도 됩니다. 의무는 `LICENSE` 파일을 지우지 않는 것 하나뿐입니다.
> 푸터의 'Powered by Jekyll with Chirpy theme' 표시는 법적 의무는 아니지만 관례로 두는 것이 좋습니다.
{: .prompt-tip }

이것도 테마 코드 수정이라, 작업하면 **POSTING_GUIDE 7절 표에 함께 적어두어야 합니다.**

### 나중에 (지금은 안 하기로 한 것)

- [ ] **첫 화면을 글 목록 대신 소개로** — 2026-08-11 에 보류
      `jekyll-paginate` 1.1 은 **루트 `index.html` 에서만** 페이지 나누기가 됩니다.
      그래서 글 목록을 다른 주소로 옮기면 "10개씩 나누기"가 깨지고,
      살리려면 플러그인을 `jekyll-paginate-v2` 로 교체해야 합니다.
      글이 10편을 넘긴 뒤 한 번에 하는 편이 낫습니다.
      (메뉴에서 소개가 맨 위로 올라가므로 당장 아쉬운 점은 크지 않습니다)
- [x] ~~**Search Console 등록**~~ → **완료** (2026-08-24). `_config.yml` 이 아니라
      최상위 HTML 파일(`googlefccb53f6e674748a.html`) 방식으로 확인했습니다

## 초안 10개 (로컬에만 있음, 깃허브에 없음)

| 파일 | 분류 |
| --- | --- |
| `_drafts/2026-08-02-civil-damages.md` | 수행기록 › 민사 |
| `_drafts/2026-08-03-criminal-defense.md` | 수행기록 › 형사 |
| `_drafts/2026-08-04-family-divorce.md` | 수행기록 › 가사 |
| `_drafts/2026-08-05-administrative-case.md` | 수행기록 › 행정 |
| `_drafts/2026-08-06-before-lawyer.md` | 이력 › 변호사가 되기 전 |
| `_drafts/2026-08-07-law-school-days.md` | 이력 › 변호사가 되기 전 |
| `_drafts/2026-08-08-my-work-today.md` | 이력 › 변호사 업무 |
| `_drafts/2026-08-09-career-interview.md` | 단상 |
| `_drafts/2026-08-10-first-visit-to-lawyer.md` | 단상 |
| `_drafts/2026-08-09-blog-open.md` | 소식 (발행했다가 되돌림) |

각 파일 맨 위 `<!-- -->` 안에 그 글을 쓸 때의 주의사항이 적혀 있습니다(사이트에는 안 보임).

이 표에 없던 `2026-08-02-civil-leakage.md`, `2026-08-03-criminal-lethal.md`,
`2026-08-04-administrative-existence.md` 는 직접 쓰신 글입니다. 앞의 둘은 2026-08-11 에,
행정 글은 2026-08-24 에 발행했습니다. 위 표의 빈 템플릿 중 날짜·분류가 겹치는 세 개
(`civil-damages.md`, `criminal-defense.md`, `administrative-case.md`)는 아직 `_drafts/` 에
그대로 남아 있습니다. 소재가 이미 발행된 글과 겹치므로, 쓸 때 내용을 새로 잡거나
템플릿을 지우는 편이 낫습니다.

## 꼭 기억할 것

- **저장소가 공개(public)입니다.** `_posts` 에 넣고 push하면 곧바로 전 세계에 공개됩니다.
  의뢰인이 특정될 수 있는 내용(이름·상호·정확한 날짜·금액·사건번호)은 옮기기 전에 반드시 지울 것
- **`_drafts` 는 이 컴퓨터에만 있습니다.** 백업되지 않으니 오래 쓰는 글은 따로 복사해 둘 것
- **깃허브 웹에서 직접 수정하지 말 것.** 로컬과 어긋나 충돌이 납니다. 작업 전 `git pull` 습관화

## 자주 쓰는 명령

```powershell
cd "C:\Users\mln\Desktop\장기프로젝트\개인페이지\archive"

# 미리보기 (초안까지 보려면 --drafts)
& "C:\Ruby34-x64\bin\ruby.exe" -S bundle exec jekyll serve --port 4001 --drafts
# → http://127.0.0.1:4001/archive/

# 올리기
git pull
git add . ; git commit -m "설명" ; git push
```

`_config.yml` 을 고쳤을 때만 서버를 껐다 켜야 합니다(`Ctrl + C` 후 재실행).
